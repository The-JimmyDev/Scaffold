# instructions.py
# This file is executed automatically after your template is copied.
# It receives these variables from scaffold.py:
#
#   project_name  (str)  — the name the user entered
#   project_path  (str)  — absolute path to the newly created project
#   yes           (bool) — True if the user passed the -y flag
#
# Use subprocess.run() to run shell commands, os / shutil for file operations,
# and print() from rich for colored output.
#
# This blank template does nothing by default.
# Add your own setup steps below.
 
print(f"'{project_name}' is ready. Go build something real.")
 
